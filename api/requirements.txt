# Kelvin AI Text Detector — API Dependencies
# Install: pip install -r requirements.txt

# Core (required)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# ML model support (optional — only needed if using trained classifier)
# Uncomment these when you have a trained model to deploy:
# scikit-learn>=1.3.0
# numpy>=1.24.0
# scipy>=1.11.0
# pandas>=2.0.0
